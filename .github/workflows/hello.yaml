name: hello-actions

on:
  push:

env:
  APP_ENV: production
  
jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello GitHub Actions"
  build:
    runs-on: ubuntu-latest
    needs: hello

    steps:
      - name: do build  
        run: echo $APP_ENV
