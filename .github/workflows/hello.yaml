name: hello-actions

on:
  push:

env:
  environment: dev

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello GitHub Actions"
  build:
    runs-on: ubuntu-latest
    needs: hello

    steps:
      - name: do build  
        run: echo ${{ vars.APP_ENV }} 
